[package]
name = "darwinbots-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
clap = { version = "4", features = ["derive"] }
darwinbots-engine = { path = "../engine" }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

